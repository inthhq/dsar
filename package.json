{
	"name": "dsar-workspace",
	"private": true,
	"description": "Developer-first DSAR engine: APIs, SDK, lifecycle, policy, and webhooks",
	"license": "Apache-2.0",
	"workspaces": {
		"packages": [
			"examples/*",
			"packages/*",
			"packages/internals/*"
		],
		"catalogs": {
			"utils": {
				"typescript": "7.0.2",
				"vitest": "4.1.10",
				"tsdown": "0.22.13"
			},
			"effect": {
				"effect": "4.0.0-beta.101",
				"@effect/platform-node": "4.0.0-beta.101",
				"@effect/language-service": "0.87.0",
				"@effect/vitest": "4.0.0-beta.101",
				"@effect/sql-pg": "4.0.0-beta.101",
				"@effect/sql-sqlite-bun": "4.0.0-beta.101",
				"@effect/sql-sqlite-node": "4.0.0-beta.101"
			},
			"types": {
				"@types/node": "24.13.3"
			}
		}
	},
	"type": "module",
	"scripts": {
		"build": "turbo run build",
		"changeset": "changeset",
		"check": "ultracite check && bun run check:docs-alpha-warning && bun run check:docs-lint && bun run check:error-docs && bun run check:tsdoc",
		"check:docs-alpha-warning": "bun ./scripts/sync-docs-alpha-warning.ts",
		"check:docs-lint": "bunx leadtype lint --src docs --error-unknown",
		"check:error-docs": "bun ./scripts/check-error-doc-coverage.ts",
		"check:tsdoc": "bun ./scripts/check-tsdoc-coverage.ts",
		"check:workspace": "turbo run check",
		"check-types": "turbo run typecheck",
		"ci:publish": "bunx changeset tag && bun publish",
		"ci:version": "bun changeset version",
		"dev": "turbo run dev",
		"docs:sync-alpha-warning": "bun ./scripts/sync-docs-alpha-warning.ts --write",
		"dsar": "bun ./packages/dsar/src/bin.ts",
		"fix": "ultracite fix",
		"format": "turbo run format",
		"format:write": "turbo run format -- --write",
		"lint": "turbo run lint",
		"prepare": "lefthook install",
		"prepare:effect": "./scripts/prepare-effect.sh",
		"release": "bun run build && rm -rf packages/dsar/docs packages/dsar/AGENTS.md packages/dsar/SKILL.md && bunx leadtype generate --bundle --src . --out packages/dsar --name dsar --enrich-git && changeset publish",
		"release:canary": "bun run build && rm -rf packages/dsar/docs packages/dsar/AGENTS.md packages/dsar/SKILL.md && bunx leadtype generate --bundle --src . --out packages/dsar --name dsar --enrich-git && changeset publish --tag canary",
		"smoke": "turbo run smoke",
		"start": "turbo run start",
		"test": "turbo run test",
		"typecheck": "turbo run typecheck",
		"version": "changeset version && bun install",
		"version:canary": "changeset version --snapshot canary && bun install",
		"version:prerelease": "changeset pre enter canary",
		"version:prerelease:exit": "changeset pre exit"
	},
	"dependencies": {
		"readie": "^1.0.0"
	},
	"devDependencies": {
		"@arethetypeswrong/core": "^0.18.5",
		"@biomejs/biome": "2.5.5",
		"@changesets/changelog-github": "^0.7.0",
		"@changesets/cli": "^2.31.1",
		"@effect/vitest": "catalog:effect",
		"@types/node": "catalog:types",
		"@typescript/typescript6": "6.0.2",
		"changeset-conventional-commits": "^1.0.4",
		"leadtype": "0.4.2",
		"lefthook": "2.1.10",
		"oxfmt": "0.60.0",
		"oxlint": "1.75.0",
		"portless": "0.15.4",
		"publint": "^0.3.22",
		"turbo": "2.10.6",
		"typescript": "catalog:utils",
		"ultracite": "7.9.4",
		"unrun": "^0.3.1"
	},
	"engines": {
		"node": ">=24 <25"
	},
	"packageManager": "bun@1.3.14",
	"patchedDependencies": {
		"leadtype@0.4.2": "patches/leadtype@0.4.2.patch"
	},
	"trustedDependencies": [
		"better-sqlite3"
	]
}
