# @dsar/internals-error-codes

Shared DSAR error code catalog consumed directly from TypeScript source.

This package intentionally exports `src` files and does not publish compiled `dist` artifacts.
