{
	"name": "@dsar/policy-engine",
	"version": "0.0.5",
	"private": true,
	"description": "Deterministic policy-pack evaluator and explainability contracts.",
	"license": "Apache-2.0",
	"type": "module",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"import": "./src/index.ts"
		}
	},
	"scripts": {
		"format": "oxfmt --check src test package.json tsconfig.json tsdown.config.ts README.md",
		"lint": "oxlint .",
		"test": "vitest run",
		"typecheck": "tsgo --noEmit"
	},
	"dependencies": {
		"@dsar/internals-error-codes": "workspace:*",
		"@dsar/schema": "workspace:*",
		"defu": "6.1.4",
		"effect": "catalog:effect"
	},
	"devDependencies": {
		"@dsar/typescript-config": "workspace:*",
		"@effect/language-service": "catalog:effect",
		"@effect/vitest": "catalog:effect",
		"tsdown": "catalog:utils",
		"typescript": "catalog:utils",
		"vitest": "catalog:utils"
	}
}
